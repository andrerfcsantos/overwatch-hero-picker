# Fail the install instead of warning when the running Node.js does not
# satisfy `engines.node` in package.json.
engine-strict=true

# Keep new dependencies on caret ranges so `vp update` can pick up
# minor/patch releases. See `npm run deps:check`.
save-prefix=^
